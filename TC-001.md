# End-to-End Validation

## Implementation Details

---

### 🔧 Pre-conditions

Uses `MongoDBClient` to ensure the product **PROD-123** exists in the `products` collection with an initial stock of **100**.

---

### 🧪 Test Steps

| Step | Area | Description |
|------|------|-------------|
| **Step 1 & 2** | API Call | Sends a `POST` request to `/api/orders` using `OrderClient`. Asserts response status is **201 Created** and retrieves the `orderId` |
| **Step 3** | Kafka / Consumer | Calls the `KafkaConsumer` endpoint for the generated `orderId` to verify the message was consumed and processed *(indirectly verifying the Kafka topic)* |
| **Step 4** | MongoDB — Orders | Queries the `orders` collection for the new record. Asserts the status is `PROCESSED` and the `customerId` matches |
| **Step 5** | MongoDB — Stock | Queries the `products` collection for `PROD-123`. Asserts that stock was decremented by the ordered quantity *(from **100** → **98**)* |

---

### ⚡ Performance Verification

Includes a **duration check** to ensure the full flow completes within a reasonable timeframe.

| Requirement | Threshold |
|-------------|-----------|
| Expected completion | `< 2s` |
| Test buffer | Allows extra time for network & async overhead |

---

> 💡 This test provides full end-to-end coverage across the **API → Kafka → Consumer → MongoDB** pipeline.